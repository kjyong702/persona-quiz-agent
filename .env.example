# 이 파일을 .env로 복사해 쓴다. .env는 커밋하지 않는다.

# DB 파일 경로 (SQLite + aiosqlite)
DATABASE_URL=sqlite+aiosqlite:///./quiz.db

# --- Phase 3(판정 파이프라인)에서 추가되는 값 ---
# OPENAI_API_KEY=sk-...
# EMBEDDING_MODEL=text-embedding-3-small
# UPPER_THRESHOLD=0.85
# LOWER_THRESHOLD=0.55
